https://approkes-default-rtdb.asia-southeast1.firebasedatabase.app/
API Key: AIzaSyCTARMTNV8J6w24RJoR91RN8D07hZxkWZ0

#include <HardwareSerial.h>
HardwareSerial MySerial(1);
int x;
void setup() {
  // put your setup code here, to run once:
  MySerial.begin(9600);
  MySerial.setTimeout(1);
  pinMode(15, OUTPUT);
}
void loop() {
    while(MySerial.available() > 0);{
      x = MySerial.readString().toInt();
      if (x == 1) {
        digitalWrite(15, HIGH);
      }
      else {
        digitalWrite(15, LOW);
      }
    }
}